{
  "name": "web3rsvp",
  "version": "1.0.0",
  "description": "RSVP dapp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "script": " npx hardhat compile && npx ts-node scripts/run.ts",
    "deploy": "npx hardhat run scripts/deploy.ts --network mumbai"
  },
  "author": "ShinyDCL",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.1",
    "hardhat": "^2.10.1"
  },
  "dependencies": {
    "dotenv": "^16.0.1"
  }
}